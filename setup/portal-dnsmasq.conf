# Captive portal DNS hijack — all queries resolve to the portal IP.
# Only listens on wlan1 (the AP interface).
# DHCP is handled by NetworkManager's shared mode, so no dhcp-range here.

interface=wlan1
bind-interfaces
no-dhcp-interface=wlan1
address=/#/192.168.4.1
