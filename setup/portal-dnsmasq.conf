# Captive portal DNS hijack — all queries resolve to the portal IP.
# Dropped into /etc/NetworkManager/dnsmasq-shared.d/ so NM's own
# dnsmasq instance (started by ipv4.method=shared) picks it up.
address=/#/192.168.4.1
